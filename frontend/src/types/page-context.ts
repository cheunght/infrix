// The page context contracts live in one discoverable types namespace.  Keep
// the legacy module as a compatibility re-export while the page components
// migrate to this path.
export * from "../page-context";
